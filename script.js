const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});
document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const tabs={work:{label:'FEATURED / 01',title:'Nocturne<br>Kitchen',copy:'A cinematic restaurant experience built around appetite, atmosphere and effortless reservations.'},thinking:{label:'PROCESS / 02',title:'Clarity<br>before polish',copy:'I begin with the user, structure the journey, then build a visual language that makes the idea memorable.'},services:{label:'CAPABILITIES / 03',title:'Design.<br>Build. Edit.',copy:'One creative partner for responsive web, product UI, short-form video and social design.'}};
document.querySelectorAll('.stage-tabs button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.stage-tabs button').forEach(b=>b.classList.remove('active'));button.classList.add('active');const item=tabs[button.dataset.tab],copy=document.querySelector('.showcase-copy');copy.classList.add('switching');setTimeout(()=>{copy.querySelector('span').textContent=item.label;copy.querySelector('h2').innerHTML=item.title;copy.querySelector('p').textContent=item.copy;copy.classList.remove('switching')},180)}));
if(!reduced&&matchMedia('(pointer:fine)').matches){const stage=document.querySelector('[data-tilt]');stage.addEventListener('pointermove',e=>{const r=stage.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;stage.style.transform=`perspective(1300px) rotateX(${-y*2.4}deg) rotateY(${x*3.2}deg) translateY(-4px)`});stage.addEventListener('pointerleave',()=>stage.style.transform='');const cursor=document.querySelector('.cursor');let mx=0,my=0,cx=0,cy=0;addEventListener('pointermove',e=>{mx=e.clientX;my=e.clientY});const follow=()=>{cx+=(mx-cx)*.16;cy+=(my-cy)*.16;cursor.style.transform=`translate3d(${cx}px,${cy}px,0) translate(-50%,-50%)`;requestAnimationFrame(follow)};follow();document.querySelectorAll('a,button,[data-cursor]').forEach(el=>{el.addEventListener('pointerenter',()=>{cursor.querySelector('span').textContent=el.dataset.cursor||'GO';cursor.classList.add('show')});el.addEventListener('pointerleave',()=>cursor.classList.remove('show'))})}
